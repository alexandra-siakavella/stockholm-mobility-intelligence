name: stockholm_mobility
version: "1.0.0"
config-version: 2

profile: stockholm_mobility

model-paths: ["models"]
test-paths: ["tests"]

models:
  stockholm_mobility:
    staging:
      +materialized: view
    marts:
      +materialized: table
      